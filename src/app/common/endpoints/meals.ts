import { environment } from "../../../environments/environment";

export const SEARCH_BY_INGREDIENTS = `${environment.apiUrl}/meals`;
export const SEARCH_RECIPE = `${environment.apiUrl}/meals/recipe`;
